#include<stdio.h>
#include"main.h"
#include<string.h>

/**
 * _strlen_recursion - function to return the length of a string
 * @s: inputed string to be used
 */
int _strlen_recursion(char *s)
{
	return strlen(s);
}

